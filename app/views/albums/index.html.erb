<script type = "text/javascript" src="/assets/contentflow.js"></script>

<div class="ContentFlow">
    <div class="loadIndicator"><div class="indicator"></div></div>
    <div class="flow">
      <% @albums.each do |alb|  %>
			<% alb.images.find_in_batches do |img| %>
				<img class="item" href= "<%= album_path(alb) %>" src="<%= img.first.url %>" title="<%= "Sol #{alb.sol}" %>"/>
			<% end %>
      <% end %>
    </div>
    <div class="globalCaption"></div>
    <div class="albumInformationForm" align="center" > Hello World
      <div><label>Album Id: </label>  </div>
      <div><label>Album Name: </label>  </div>
      <div><label>Album Pictures: </label>  </div>
      <div><label>Album Id: </label>  </div>
    
    </div>
    <div class="scrollbar"><div class="slider"><div class="position"></div></div></div>

</div>


