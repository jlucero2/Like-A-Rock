<%= form_for(@deohtest) do |f| %>
  <% if @deohtest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deohtest.errors.count, "error") %> prohibited this deohtest from being saved:</h2>

      <ul>
      <% @deohtest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
